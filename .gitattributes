# Unified diff context lines intentionally consist of a single space.
# Keep patch context identical to the LF source used by the host assembler,
# including on Windows checkouts with core.autocrlf enabled.
*.patch text eol=lf -whitespace
